#ifndef TESTS_HPP
#define TESTS_HPP

/**
 * Test the magic cast in every ways possible.
 */
void	magic_cast_test(void);

/**
 * Test some getters and setters for the configuration.
 *
 * @param conf The instance of the HttpConfig class.
 */
void	directive_get_test(cfg::HttpConfig& conf);

#endif